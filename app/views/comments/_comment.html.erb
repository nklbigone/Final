
  <% @comments.each do |comment| %>
      
        <p><br><%= comment.content %><br>
        <%= link_to "delete", [comment.grade, comment], method: :delete, class:'btn-danger btn-sm' %>
        <%= link_to "edit ", edit_grade_comment_path(comment.grade, comment), class:'btn-info btn-sm' %></p>
  <% end %>
<%= render 'comments/form'%>
